apiVersion: v1
kind: Service
metadata:
  name: p-service
  labels:
    app: modulo-p
spec:
  type: NodePort
  selector:
    app: modulo-p
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080