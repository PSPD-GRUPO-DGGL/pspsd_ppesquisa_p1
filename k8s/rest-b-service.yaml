apiVersion: v1
kind: Service
metadata:
  name: rest-b-service
  labels:
    app: rest-b
spec:
  type: ClusterIP
  selector:
    app: rest-b
  ports:
    - name: http
      protocol: TCP
      port: 8002
      targetPort: 8002
